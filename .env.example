# Hackathon Starter Kit - Environment Configuration
# Copy to .env and customize

# === DEVELOPMENT MODE (Default) ===
NODE_ENV=development
DOMAIN=localhost
FRONTEND_PORT=3000
BACKEND_PORT=8000
LOG_LEVEL=info

# Database (use external service like Supabase, PlanetScale, etc.)
DATABASE_URL=postgresql://user:pass@localhost:5432/hackathon

# API Security
API_KEY=your-secret-api-key-here

# === SCALING/PRODUCTION MODE ===
# Uncomment these when ready to scale:

# Enable HTTPS routing through Traefik
# ENABLE_HTTPS=true

# Your domain (for production)
# DOMAIN=your-app.hackathon.live

# === CLOUDFLARE TUNNEL (Optional) ===
# For public URLs during development
# TUNNEL_TOKEN=your-cloudflare-tunnel-token

# === TIME ZONE ===
TZ=UTC
